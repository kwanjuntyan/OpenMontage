"""Reserved Workspace v1 API layer.

No router is registered during B0.0B.  When B0.2 is authorized, this layer may
consume shared projections but must not read canonical files or decide source
authority itself.
"""
